# ─── 필수 ─────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-proj-...
OLLAMA_BASE_URL=http://localhost:11434

# ─── 선택 ─────────────────────────────────────────────────────────
# OPENAI_BASE_URL=https://your-proxy.example.com/v1
# OPENAI_MODEL=gpt-5.1

# 절대경로 권장 (상대경로는 CWD에 따라 다른 DB를 가리킴)
JCQ_DB_URL=sqlite:////absolute/path/to/backend/data/jcq.db

JCQ_QUEUE_CONCURRENCY=1
# JCQ_SUBMIT_COOLDOWN_S=10

# DEV ONLY — 3-LLM 앙상블 단계 스킵 (Ollama 없이 개발할 때).
# 샌드박스 테스트는 동일하게 채점됨. 프로덕션에서는 절대 사용 금지.
# JCQ_SKIP_ENSEMBLE=1

# ─── 인증 (Supabase) ───────────────────────────────────────────────
# ES256 비대칭 키 검증용 — JWKS 엔드포인트 base URL
SUPABASE_URL=https://your-project.supabase.co
# HS256 레거시 프로젝트만 필요
# SUPABASE_JWT_SECRET=...

# 개발/테스트 전용
# JCQ_AUTH_ALLOW_DEV_STUB=1
# JCQ_COOKIE_INSECURE=1

# ─── 테스트 전용 ────────────────────────────────────────────────────
# JCQ_RUN_LIVE_LLM=1
